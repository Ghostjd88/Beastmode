let deferredInstallPrompt=null;
window.addEventListener('beforeinstallprompt',event=>{event.preventDefault();deferredInstallPrompt=event;if(document.getElementById('page-bmi')?.classList.contains('active'))rBMI()});
window.addEventListener('appinstalled',()=>{deferredInstallPrompt=null;toast('BEASTMODE installed ✓')});
function pwaSettingsHTML(){const standalone=window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true;return`<div class="setting-row"><div><strong>Offline app</strong><div>${standalone?'Installed on this device':navigator.onLine?'Ready to install and use offline':'Offline mode active'}</div></div><div class="setting-actions">${standalone?'':'<button class="primary-mini" onclick="installBeastmodeApp()">Install app</button>'}<button onclick="refreshOfflineCache()">Refresh offline data</button></div></div>`}
async function installBeastmodeApp(){if(deferredInstallPrompt){deferredInstallPrompt.prompt();await deferredInstallPrompt.userChoice;deferredInstallPrompt=null;return}toast('Use your browser menu and choose Add to Home Screen')}
async function refreshOfflineCache(){if(!('serviceWorker'in navigator)){toast('Offline mode is not supported in this browser');return}const registration=await navigator.serviceWorker.getRegistration();await registration?.update();toast('Offline files refreshed ✓')}
function initPWA(){if('serviceWorker'in navigator)navigator.serviceWorker.register('./service-worker.js?v=17').catch(()=>{})}
