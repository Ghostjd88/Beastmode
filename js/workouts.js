const workoutId=prefix=>`${prefix}-${Date.now()}-${Math.random().toString(36).slice(2,8)}`;
const workoutDateKey=()=>nowGMT4().toISOString().slice(0,10);
const LB_PER_KG=2.2046226218;
let restTimerInterval=0;
const workoutUnit=()=>S.workoutUnit==='kg'?'kg':'lb';
const displayWeight=value=>{const converted=workoutUnit()==='kg'?(Number(value)||0)/LB_PER_KG:Number(value)||0;return Math.round(converted*10)/10};
const storedWeight=value=>workoutUnit()==='kg'?(Number(value)||0)*LB_PER_KG:Number(value)||0;
const weightText=value=>`${displayWeight(value).toLocaleString(undefined,{maximumFractionDigits:1})} ${workoutUnit()}`;
const convertedVolume=value=>Math.round(workoutUnit()==='kg'?(Number(value)||0)/LB_PER_KG:Number(value)||0);
const displayVolume=value=>convertedVolume(value).toLocaleString();
const formatDuration=seconds=>{const total=Math.max(0,Math.floor(Number(seconds)||0)),hours=Math.floor(total/3600),minutes=Math.floor(total%3600/60);return hours?`${hours}h ${minutes}m`:`${Math.max(1,minutes)} min`};
function groupWorkoutRows(rows){const sections=[];let current=null;rows.forEach((row,index)=>{if(row.day){current={day:row.day,rows:[{...row,idx:index}]};sections.push(current)}else if(current)current.rows.push({...row,idx:index});else{current={day:'Workout',rows:[{...row,idx:index}]};sections.push(current)}});return sections}
function activeCustomRoutine(){return(S.customRoutines||[]).find(routine=>routine.id===S.activeRoutineId)||null}
function workoutContext(){const custom=activeCustomRoutine(),colors={fase1:C().r,fase2:C().a,fase3:C().t};if(custom){const rows=custom.exercises.map((exercise,index)=>({day:index===0?custom.name:'',exercises:exercise.name,reps:exercise.reps,notes:exercise.notes,idx:index,id:exercise.id}));return{type:'custom',name:custom.name,color:'#7c5cfc',rows,sections:rows.length?groupWorkoutRows(rows):[]}}const phase=S.activePhase||'fase1',names={fase1:i('p1'),fase2:i('p2'),fase3:i('p3')},rows=S.workouts[phase]||[];return{type:'phase',name:names[phase],color:colors[phase],rows,sections:groupWorkoutRows(rows)}}
function selectWorkoutRoutine(value){if(value.startsWith('phase:')){S.activeRoutineId='';S.activePhase=value.slice(6)}else S.activeRoutineId=value;save();rWork()}
function setWorkoutUnit(value){S.workoutUnit=value==='kg'?'kg':'lb';save();rWork();toast(`Workout weights set to ${workoutUnit()}`)}
function openRoutineModal(){document.getElementById('routine-modal')?.remove();const modal=document.createElement('div');modal.id='routine-modal';modal.className='modal-overlay';modal.onclick=event=>{if(event.target===modal)modal.remove()};modal.innerHTML=`<div class="modal-sheet"><div class="modal-handle"></div><div class="modal-title">Create custom routine</div><div class="modal-sub">Build a reusable workout and add movements directly from the Exercise Guide.</div><input class="scan-field" id="routine-name" aria-label="Routine name" maxlength="120" placeholder="Example: Push Day"><button class="scan-btn" onclick="createRoutine()">Create routine</button><button class="scan-btn secondary" onclick="document.getElementById('routine-modal').remove()">Cancel</button></div>`;document.body.appendChild(modal);document.getElementById('routine-name').focus()}
function createRoutine(){const input=document.getElementById('routine-name'),name=input.value.trim();if(!name){input.setAttribute('aria-invalid','true');toast('Enter a routine name');return}const routine={id:workoutId('routine'),name,exercises:[]};S.customRoutines.push(routine);S.activeRoutineId=routine.id;save();document.getElementById('routine-modal')?.remove();rWork();toast('Custom routine created ✓')}
function renameActiveRoutine(value){const routine=activeCustomRoutine();if(!routine)return;routine.name=value.slice(0,120);save()}
function duplicateActiveRoutine(){const routine=activeCustomRoutine();if(!routine)return;const copy={...clone(routine),id:workoutId('routine'),name:`${routine.name} Copy`.slice(0,120)};S.customRoutines.splice(S.customRoutines.indexOf(routine)+1,0,copy);S.activeRoutineId=copy.id;save();rWork();toast('Routine duplicated ✓')}
function moveActiveRoutine(direction){const routine=activeCustomRoutine(),index=S.customRoutines.indexOf(routine),target=index+direction;if(!routine||target<0||target>=S.customRoutines.length)return;[S.customRoutines[index],S.customRoutines[target]]=[S.customRoutines[target],S.customRoutines[index]];save();rWork()}
function deleteActiveRoutine(){const routine=activeCustomRoutine();if(!routine||!confirm(`Delete ${routine.name}? Workout history will be kept.`))return;S.customRoutines=S.customRoutines.filter(item=>item.id!==routine.id);S.activeRoutineId='';save();rWork();toast('Routine deleted')}
function updateRoutineRow(index,field,value){const routine=activeCustomRoutine();if(routine){const map={exercises:'name',reps:'reps',notes:'notes'};routine.exercises[index][map[field]]=value}else S.workouts[S.activePhase][index][field]=value;save()}
function moveRoutineExercise(index,direction){const routine=activeCustomRoutine();if(!routine)return;const target=index+direction;if(target<0||target>=routine.exercises.length)return;[routine.exercises[index],routine.exercises[target]]=[routine.exercises[target],routine.exercises[index]];save();rWork()}
function removeRoutineRow(index){const routine=activeCustomRoutine();if(routine)routine.exercises.splice(index,1);else{const rows=S.workouts[S.activePhase],day=rows[index]?.day||'';rows.splice(index,1);if(day&&rows[index]&&!rows[index].day)rows[index].day=day}save();rWork()}
function addBlankExercise(){const routine=activeCustomRoutine();if(routine)routine.exercises.push({id:'',name:'New exercise',reps:'3x10',notes:''});else S.workouts[S.activePhase].push({day:i('nd'),exercises:'New exercise',reps:'3x10',notes:''});save();rWork()}
function addGuideExerciseToRoutine(id){const exercise=exerciseGuide.data?.find(item=>item.id===String(id));if(!exercise)return;const routine=activeCustomRoutine();if(routine){if(routine.exercises.some(item=>guideNorm(item.name)===guideNorm(exercise.name))){toast('Exercise is already in this routine');return}routine.exercises.push({id:exercise.id,name:exercise.name,reps:'3x10',notes:exercise.target})}else{const rows=S.workouts[S.activePhase];if(rows.some(item=>guideNorm(item.exercises)===guideNorm(exercise.name))){toast('Exercise is already in this phase');return}rows.push({day:'',exercises:exercise.name,reps:'3x10',notes:exercise.target})}save();closeExerciseGuide();rWork();toast(`Added ${exercise.name} ✓`)}
function previousExercisePerformance(name){for(const log of [...(S.workoutLogs||[])].reverse()){const exercise=log.exercises.find(item=>guideNorm(item.name)===guideNorm(name));if(exercise){const done=exercise.sets.filter(set=>set.done);if(done.length)return{date:log.date,sets:done,max:Math.max(...done.map(set=>Number(set.weight)||0)),reps:done.map(set=>set.reps).join('/')}}}return null}
function progressionRecommendation(name,target){const previous=previousExercisePerformance(name);if(!previous)return'First logged session — choose a comfortable starting load.';const log=[...(S.workoutLogs||[])].reverse().flatMap(item=>item.exercises).find(item=>guideNorm(item.name)===guideNorm(name)),done=log?.sets.filter(set=>set.done)||[],range=String(target).match(/(\d+)\s*x\s*(\d+)(?:-(\d+))?/i),required=Number(range?.[1]||1),top=Number(range?.[3]||range?.[2]||0),allHit=top&&done.length>=required&&done.every(set=>Number(set.reps)>=top),weight=previous.max,next=weight+(workoutUnit()==='kg'?2.5*LB_PER_KG:5);return allHit&&weight?`Progressive overload: try ${weightText(next)} next time.`:weight?`Repeat ${weightText(weight)} and improve form or reps.`:'Build consistency before increasing load.'}
function startWorkout(sectionIndex){const context=workoutContext(),section=context.sections[sectionIndex];if(!section||!section.rows.length)return;if(S.activeWorkout&&!confirm('Replace the workout currently in progress?'))return;S.activeWorkout={id:workoutId('session'),date:workoutDateKey(),startedAt:new Date().toISOString(),completedAt:'',routineName:context.name,day:section.day,volume:0,durationSeconds:0,prCount:0,restTimerEnd:0,restSeconds:S.restTimerSeconds||90,exercises:section.rows.filter(row=>row.exercises.trim()).map(row=>{const count=Math.min(10,Math.max(1,Number.parseInt(String(row.reps).match(/\d+/)?.[0],10)||3)),previous=previousExercisePerformance(row.exercises);return{name:row.exercises,target:row.reps,notes:row.notes,sets:Array.from({length:count},()=>({weight:previous?.max||0,reps:0,done:false}))}})};S.lastWorkoutSummaryId='';save();rWork();document.getElementById('active-workout')?.scrollIntoView({behavior:'smooth',block:'start'});toast('Workout started')}
function updateActiveSet(exerciseIndex,setIndex,key,value){const set=S.activeWorkout?.exercises?.[exerciseIndex]?.sets?.[setIndex];if(!set)return;set[key]=key==='done'?Boolean(value):key==='weight'?Math.min(5000,Math.max(0,storedWeight(value))):Math.min(1000,Math.max(0,Number(value)||0));save();if(key==='done'){if(set.done)startRestTimer();rWork()}}
function adjustActiveSet(exerciseIndex,setIndex,key,delta){const set=S.activeWorkout?.exercises?.[exerciseIndex]?.sets?.[setIndex];if(!set)return;const current=key==='weight'?displayWeight(set.weight):Number(set.reps)||0;updateActiveSet(exerciseIndex,setIndex,key,Math.max(0,current+delta));rWork()}
function addActiveSet(exerciseIndex){const exercise=S.activeWorkout?.exercises?.[exerciseIndex];if(!exercise)return;const last=exercise.sets.at(-1)||{};exercise.sets.push({weight:Number(last.weight)||0,reps:Number(last.reps)||0,done:false});save();rWork()}
function removeActiveSet(exerciseIndex,setIndex){const exercise=S.activeWorkout?.exercises?.[exerciseIndex];if(!exercise||exercise.sets.length<=1)return;exercise.sets.splice(setIndex,1);save();rWork()}
function openExerciseSubstitution(exerciseIndex){const exercise=S.activeWorkout?.exercises?.[exerciseIndex];if(exercise)openExerciseGuide(exercise.name,{type:'replace',exerciseIndex})}
function replaceActiveExercise(exerciseIndex,id){const current=S.activeWorkout?.exercises?.[exerciseIndex],replacement=exerciseGuide.data?.find(item=>item.id===String(id));if(!current||!replacement)return;const previous=previousExercisePerformance(replacement.name);current.name=replacement.name;current.notes=`Substitution · ${replacement.target}`;current.sets=current.sets.map(()=>({weight:previous?.max||0,reps:0,done:false}));save();closeExerciseGuide();rWork();toast(`Substituted ${replacement.name} ✓`)}
function startRestTimer(seconds=S.activeWorkout?.restSeconds||S.restTimerSeconds||90){if(!S.activeWorkout)return;const duration=Math.min(600,Math.max(15,Number(seconds)||90));S.activeWorkout.restSeconds=duration;S.restTimerSeconds=duration;S.activeWorkout.restTimerEnd=Date.now()+duration*1000;save();syncRestTimer()}
function adjustRestTimer(delta){if(!S.activeWorkout)return;const remaining=Math.max(0,Math.ceil((S.activeWorkout.restTimerEnd-Date.now())/1000)),next=Math.min(600,Math.max(15,(remaining||S.activeWorkout.restSeconds||90)+delta));startRestTimer(next)}
function stopRestTimer(){if(!S.activeWorkout)return;S.activeWorkout.restTimerEnd=0;save();syncRestTimer()}
function notifyRestComplete(){try{navigator.vibrate?.([180,80,180]);const AudioContext=window.AudioContext||window.webkitAudioContext,context=new AudioContext(),oscillator=context.createOscillator(),gain=context.createGain();oscillator.connect(gain);gain.connect(context.destination);oscillator.frequency.value=880;gain.gain.value=.08;oscillator.start();oscillator.stop(context.currentTime+.25)}catch{}toast('Rest complete — next set!')}
function syncRestTimer(){const timer=document.getElementById('rest-timer-value'),workout=S.activeWorkout;if(!timer||!workout)return;const remaining=workout.restTimerEnd?Math.max(0,Math.ceil((workout.restTimerEnd-Date.now())/1000)):0;timer.textContent=remaining?`${Math.floor(remaining/60)}:${String(remaining%60).padStart(2,'0')}`:'Ready';timer.closest('.rest-timer')?.classList.toggle('active',remaining>0);if(workout.restTimerEnd&&remaining===0){workout.restTimerEnd=0;save();notifyRestComplete()}}
function initWorkoutFlow(){clearInterval(restTimerInterval);restTimerInterval=setInterval(syncRestTimer,1000);syncRestTimer()}
function cancelActiveWorkout(){if(!S.activeWorkout||!confirm('Discard this workout in progress?'))return;S.activeWorkout=null;save();rWork()}
function workoutPRCount(workout){return workout.exercises.reduce((count,exercise)=>{const done=exercise.sets.filter(set=>set.done),max=done.length?Math.max(...done.map(set=>Number(set.weight)||0)):0,previous=previousExercisePerformance(exercise.name)?.max||0;return count+(max>previous?1:0)},0)}
function finishWorkout(){const workout=S.activeWorkout;if(!workout)return;const completedSets=workout.exercises.flatMap(exercise=>exercise.sets).filter(set=>set.done);if(!completedSets.length){toast('Complete at least one set before finishing');return}workout.completedAt=new Date().toISOString();workout.durationSeconds=Math.max(1,Math.round((Date.parse(workout.completedAt)-Date.parse(workout.startedAt))/1000));workout.volume=workout.exercises.reduce((total,exercise)=>total+exercise.sets.filter(set=>set.done).reduce((sum,set)=>sum+(Number(set.weight)||0)*(Number(set.reps)||0),0),0);workout.prCount=workoutPRCount(workout);workout.restTimerEnd=0;S.workoutLogs.push(workout);S.workoutLogs=S.workoutLogs.slice(-500);S.lastWorkoutSummaryId=workout.id;const dayNumber=Number(workout.day.match(/Day\s+(\d+)/i)?.[1]),week=S.activeProgressWeek+1,slot=dayNumber>=1&&dayNumber<=6?`Day ${dayNumber}`:SS.find(day=>!S.checklist[`${day}_w${week}`]);if(slot)S.checklist[`${slot}_w${week}`]=true;S.activeWorkout=null;save();rWork();toast(`Workout saved · ${displayVolume(workout.volume)} ${workoutUnit()} volume ✓`)}
function dismissWorkoutSummary(){S.lastWorkoutSummaryId='';save();rWork()}
function workoutSummaryHTML(){const log=(S.workoutLogs||[]).find(item=>item.id===S.lastWorkoutSummaryId);if(!log)return'';const sets=log.exercises.flatMap(exercise=>exercise.sets).filter(set=>set.done).length;return`<section class="card workout-summary"><div class="session-head"><div><div class="sec-label" style="margin:0">Workout complete</div><div class="session-title">${h(log.day||log.routineName)}</div></div><button aria-label="Close workout summary" onclick="dismissWorkoutSummary()">×</button></div><div class="summary-grid"><div><strong>${formatDuration(log.durationSeconds)}</strong><span>Duration</span></div><div><strong>${sets}</strong><span>Sets</span></div><div><strong>${displayVolume(log.volume)}</strong><span>${workoutUnit()} volume</span></div><div><strong>${log.prCount||0}</strong><span>New PRs</span></div></div></section>`}
function activeWorkoutHTML(){const workout=S.activeWorkout;if(!workout)return'';const elapsed=Math.max(0,Math.round((Date.now()-Date.parse(workout.startedAt))/1000));return`<section class="card active-session" id="active-workout"><div class="resume-banner">Session safely saved · ${formatDuration(elapsed)} elapsed</div><div class="session-head"><div><div class="sec-label" style="margin:0">Workout in progress</div><div class="session-title">${h(workout.day||workout.routineName)}</div></div><button class="danger-link" onclick="cancelActiveWorkout()">Discard</button></div><div class="rest-timer"><div><span>Rest timer</span><strong id="rest-timer-value">Ready</strong></div><div><button onclick="adjustRestTimer(-15)">−15s</button><button class="timer-start" onclick="startRestTimer()">Start ${workout.restSeconds||S.restTimerSeconds||90}s</button><button onclick="adjustRestTimer(15)">+15s</button><button onclick="stopRestTimer()">Skip</button></div></div>${workout.exercises.map((exercise,exerciseIndex)=>{const previous=previousExercisePerformance(exercise.name);return`<div class="session-exercise"><div class="session-exercise-head"><div><strong>${h(exercise.name)}</strong><div class="session-meta">Target ${h(exercise.target)}${previous?` · Previous ${weightText(previous.max)} · ${h(previous.reps)} reps`:''}</div></div><div class="exercise-actions"><button onclick="openExerciseGuide(decodeURIComponent('${enc(exercise.name)}'))">Guide</button><button onclick="openExerciseSubstitution(${exerciseIndex})">Substitute</button></div></div><div class="progression-tip">${h(progressionRecommendation(exercise.name,exercise.target))}</div><div class="set-grid set-grid-head"><span>Set</span><span>Weight ${workoutUnit()}</span><span>Reps</span><span>Done</span><span></span></div>${exercise.sets.map((set,setIndex)=>`<div class="set-grid ${set.done?'set-complete':''}"><span>${setIndex+1}</span><div class="step-input"><button aria-label="Decrease ${h(exercise.name)} set ${setIndex+1} weight" onclick="adjustActiveSet(${exerciseIndex},${setIndex},'weight',${workoutUnit()==='kg'?-2.5:-5})">−</button><input aria-label="${h(exercise.name)} set ${setIndex+1} weight" type="number" min="0" max="5000" step="${workoutUnit()==='kg'?'0.5':'1'}" inputmode="decimal" value="${displayWeight(set.weight)||''}" onchange="updateActiveSet(${exerciseIndex},${setIndex},'weight',this.value)"><button aria-label="Increase ${h(exercise.name)} set ${setIndex+1} weight" onclick="adjustActiveSet(${exerciseIndex},${setIndex},'weight',${workoutUnit()==='kg'?2.5:5})">+</button></div><div class="step-input"><button aria-label="Decrease ${h(exercise.name)} set ${setIndex+1} reps" onclick="adjustActiveSet(${exerciseIndex},${setIndex},'reps',-1)">−</button><input aria-label="${h(exercise.name)} set ${setIndex+1} reps" type="number" min="0" max="1000" inputmode="numeric" value="${set.reps||''}" onchange="updateActiveSet(${exerciseIndex},${setIndex},'reps',this.value)"><button aria-label="Increase ${h(exercise.name)} set ${setIndex+1} reps" onclick="adjustActiveSet(${exerciseIndex},${setIndex},'reps',1)">+</button></div><label class="done-toggle"><input aria-label="Complete ${h(exercise.name)} set ${setIndex+1}" type="checkbox" ${set.done?'checked':''} onchange="updateActiveSet(${exerciseIndex},${setIndex},'done',this.checked)"><span>✓</span></label><button class="set-remove" aria-label="Remove ${h(exercise.name)} set ${setIndex+1}" onclick="removeActiveSet(${exerciseIndex},${setIndex})">×</button></div>`).join('')}<button class="mini-action" onclick="addActiveSet(${exerciseIndex})">+ Add set</button></div>`}).join('')}<button class="scan-btn sticky-finish" onclick="finishWorkout()">Finish and save workout</button></section>`}
function workoutHistoryHTML(){const logs=[...(S.workoutLogs||[])].reverse().slice(0,5);if(!logs.length)return'<div class="empty">Your completed sessions will appear here.</div>';return logs.map(log=>`<div class="history-row"><div><strong>${h(log.day||log.routineName)}</strong><div>${h(log.date)} · ${log.exercises.length} exercises · ${formatDuration(log.durationSeconds)}</div></div><span>${displayVolume(log.volume||0)} ${workoutUnit()}</span></div>`).join('')}
function routineRowHTML(row,custom){const previous=previousExercisePerformance(row.exercises);return`<div class="routine-row"><div><input aria-label="Exercise name" value="${h(row.exercises)}" oninput="updateRoutineRow(${row.idx},'exercises',this.value)"><button class="guide-link" onclick="openExerciseGuide(decodeURIComponent('${enc(row.exercises)}'))">View guide${previous?` · last ${weightText(previous.max)}`:''}</button></div><input aria-label="Sets and repetitions" value="${h(row.reps)}" oninput="updateRoutineRow(${row.idx},'reps',this.value)"><input aria-label="Exercise notes" value="${h(row.notes)}" placeholder="notes" oninput="updateRoutineRow(${row.idx},'notes',this.value)"><div class="row-actions">${custom?`<button aria-label="Move ${h(row.exercises)} up" onclick="moveRoutineExercise(${row.idx},-1)">↑</button><button aria-label="Move ${h(row.exercises)} down" onclick="moveRoutineExercise(${row.idx},1)">↓</button>`:''}<button class="row-remove" aria-label="Remove ${h(row.exercises)}" onclick="removeRoutineRow(${row.idx})">×</button></div></div>`}
function rWork(){const context=workoutContext(),custom=activeCustomRoutine(),selection=custom?.id||`phase:${S.activePhase||'fase1'}`;document.getElementById('page-workout').innerHTML=`${workoutSummaryHTML()}${activeWorkoutHTML()}<div class="workout-actions"><button class="scan-btn secondary" onclick="openExerciseGuide()">Exercise Guide · 1,324</button><button class="scan-btn secondary" onclick="openRoutineModal()">+ Custom routine</button></div><div class="workout-preferences"><label>Workout unit <select aria-label="Workout weight unit" onchange="setWorkoutUnit(this.value)"><option value="lb" ${workoutUnit()==='lb'?'selected':''}>Pounds (lb)</option><option value="kg" ${workoutUnit()==='kg'?'selected':''}>Kilograms (kg)</option></select></label><span>Active sessions save automatically on this device.</span></div><div class="sec-label">Workout routine</div><div class="routine-toolbar"><select aria-label="Select workout routine" onchange="selectWorkoutRoutine(this.value)"><option value="phase:fase1" ${selection==='phase:fase1'?'selected':''}>Phase 1 · Reactivation</option><option value="phase:fase2" ${selection==='phase:fase2'?'selected':''}>Phase 2 · Main Program</option><option value="phase:fase3" ${selection==='phase:fase3'?'selected':''}>Phase 3 · Progressive Overload</option>${S.customRoutines.map(routine=>`<option value="${h(routine.id)}" ${selection===routine.id?'selected':''}>Custom · ${h(routine.name)}</option>`).join('')}</select>${custom?`<input aria-label="Custom routine name" value="${h(custom.name)}" maxlength="120" oninput="renameActiveRoutine(this.value)"><div class="routine-actions"><button onclick="moveActiveRoutine(-1)" aria-label="Move routine up">↑</button><button onclick="moveActiveRoutine(1)" aria-label="Move routine down">↓</button><button onclick="duplicateActiveRoutine()">Duplicate</button><button class="danger-link" onclick="deleteActiveRoutine()">Delete</button></div>`:''}</div>${context.sections.length?context.sections.map((section,sectionIndex)=>`<section class="card routine-card" style="border-top-color:${context.color}"><div class="routine-card-head"><div class="session-title" style="color:${context.color}">${h(section.day)}</div><button class="start-workout-btn" onclick="startWorkout(${sectionIndex})">${S.activeWorkout?'Start instead':'Start workout'}</button></div><div class="routine-table"><div class="routine-row routine-row-head"><span>Exercise</span><span>Sets × reps</span><span>Notes</span><span></span></div>${section.rows.map(row=>routineRowHTML(row,Boolean(custom))).join('')}</div></section>`).join(''):'<div class="empty card">This routine is empty. Add an exercise from the guide or create a blank row.</div>'}<button class="add-btn" onclick="addBlankExercise()">+ Add blank exercise</button><div class="sec-label" style="margin-top:18px">Recent workout history</div><div class="card">${workoutHistoryHTML()}</div>`;syncRestTimer()}
